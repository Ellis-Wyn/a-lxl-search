# Zeabur 部署配置
version: 1

# 服务类型识别
type: "python"

# 构建配置
build:
  # 构建命令
  command: "pip install -r requirements.txt"

# 部署配置
deploy:
  # 启动命令
  command: "uvicorn main:app --host 0.0.0.0 --port $PORT"

# 环境变量
environment:
  - PORT=8000
